%nav
  =link_to "CMS - alpha", root_path, class: "navbar-brand"
  #main-nav
    %ul
      -@menus.find { |menu| menu.nam == "Main Nav" }.menu_items.each do |item|
        %li
          =link_to item.titel, item.url